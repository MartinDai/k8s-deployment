apiVersion: v1
kind: Service
metadata:
  name: wechat-robot-service
  namespace: wechat-robot
spec:
  type: LoadBalancer
  selector:
    app: wechat-robot
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080